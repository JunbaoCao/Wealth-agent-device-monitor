# Wealth device-inspector · 项目架构白皮书

> **版本：v1.3.0（2026-08-14）**
> **作者：威尔思（Wealth）会计师事务所**
> **状态：架构总纲 — 说明本项目的定位、分层、目录、数据流与扩展方式**

---

## 一、项目定位

**Wealth device-inspector（设备监察）** 是一个开源的**设备环境监察框架**，是智能体（Agent）的**架构底层**。

它的职责不是审计财务，而是**检查机器环境**——让 agent 不会被自己的系统绊倒：编码错、区域错、运行时缺、文本乱码。任何人（哪怕不懂命令）clone 下来跑一个脚本，就能**了解自己电脑的全貌**并生成专属的 `设备档案.md`。

**核心思想：agent 先了解自己的机器，再行动。**

---

## 二、架构分层

```
┌─────────────────────────────────────────────────┐
│  第1层 入口层（用户怎么用）                        │
│  README.md / README.zh.md / 一键扫描脚本          │
└───────────────────────┬─────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────┐
│  第2层 能力层（核心能力）                          │
│  scripts/（扫描） · agent/（人设） · SKILL.md      │
│  device-monitor-plugin/（后台守护）                │
└───────────────────────┬─────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────┐
│  第3层 规范层（格式/方法论）                       │
│  format/（输出模板·编码规范）                     │
│  docs/（通用技能·RAG·后台守护·文件归属）           │
└───────────────────────┬─────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────┐
│  第4层 产出层（运行后动态生成）                    │
│  设备档案.md · 日志/ · 流程备份/ · 知识库/          │
└─────────────────────────────────────────────────┘
```

**关键原则：工具是通用的（第1-3层，进仓库），数据是动态生成的（第4层，留在本机）。**

---

## 三、目录结构（架构地图）

```
Wealth-device-inspector/
│
├── README.md / README.zh.md   ← 入口：怎么用、怎么部署（双语）
├── WHITEPAPER.md              ← 本文件：架构总纲
├── LICENSE                    ← MIT 许可
├── SKILL.md                   ← 技能定义（供AI技能目录发现）
│
├── agent/                     ← 人设层
│   └── SOUL.md                ← 设备监察人设（通用版，苏格拉底纠错+有底线）
│
├── device-monitor-plugin/     ← 后台守护层（DeepSeek Harness 动态插件）
│   ├── plugin.js              ← 心跳/日志到Obsidian/check_device工具/技能注册
│   └── package.json           ← 插件元数据
│
├── scripts/                   ← 扫描层（跨平台，一键）
│   ├── scan-device.ps1        ← Windows PowerShell
│   └── scan-device.py         ← Python（Win/Mac/Linux）
│
├── format/                    ← 规范层（输出格式）
│   ├── 设备档案模板.md         ← 空模板（脚本填这个）
│   └── 格式与编码.md           ← 编码/换行/时区通用规范
│
└── docs/                      ← 方法论层
    ├── 通用技能.md            ← 换电脑照做的方法论（6步）
    ├── 知识图谱与RAG.md       ← 知识库/RAG 接入框架
    ├── 后台守护与自愈.md       ← 后台静默守护与自愈说明
    └── 文件清单与归属.md       ← 每个文件该放本地还是仓库
```

---

## 四、数据流（一次完整的监察）

```
用户 clone 仓库
   │
   ▼
跑 scripts/scan-device.ps1（或 .py）     ← 第2层 扫描
   │
   ▼
按 format/设备档案模板.md 填数据          ← 第3层 规范
   │
   ▼
生成 设备档案.md（动态，留本机）           ← 第4层 产出
   │
   ▼
（可选）挂 agent/SOUL.md → 监察师智能体     ← 第2层 人设
   │
   ▼
（可选）接 RAG → 知识库可查询              ← 第3层 RAG 文档
   │
   ▼
（可选）后台守护插件 → 静默心跳+日志+自愈   ← 第2层 插件
```

---

## 五、安全与隐私边界

- **第4层（数据）永远留本机**，绝不进仓库。
- **第1-3层（工具/方法）进公开仓库**，可自由共享。
- agent 只"查"和"讲"，不擅自改系统；查不到标"待手动确认"。
- 敏感数据（审计报告、财务数据）建议本地 qwen 嵌入，不上传云端 RAG。

---

## 六、当前状态（v1.3.0，2026-08-14）

| 模块 | 状态 |
|------|------|
| 扫描脚本（ps1/py） | ✅ 可用，跨平台 |
| 设备档案模板 + 编码规范 | ✅ 就位 |
| 监察师人设（SOUL） | ✅ 就位（苏格拉底纠错+有底线） |
| 后台守护插件 | ✅ 就位（可加载） |
| 通用技能（6步方法论） | ✅ 就位 |
| RAG 接入框架 | ✅ 就位（qwen嵌入+OCR+硅基流动rerank） |
| 文件归属清单 | ✅ 就位 |
| README 双语部署说明 | ✅ 就位 |
| **本白皮书（架构总纲）** | ✅ v1.3.0 |

---

## 七、如何扩展

| 想加的能力 | 放哪层 | 怎么做 |
|-----------|--------|--------|
| 更多设备检查项 | 第2层 scripts/ | 在 ps1/py 加扫描逻辑 |
| 新文件格式转换 | 第2层 scripts/ + 第3层 format/ | 加转换脚本+模板 |
| 新行业 skill | 第3层 docs/ | 按 SKILL.md 格式仿写 |
| 自动维修规则 | 第2层 device-monitor-plugin/ | 在插件加自愈规则 |
| 多语言版本 | 第1层 README | 加 README.xx.md |

---

*威尔思（Wealth）会计师事务所出品 · MIT License · 自由复用*